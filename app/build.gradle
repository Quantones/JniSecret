plugins {
    id 'com.android.application'
    id 'kotlin-android'
    id 'io.github.quantones.harpocrate.jni-secret'
}

android {
    compileSdk 31
    buildToolsVersion = "31.0.0"

    defaultConfig {
        applicationId "com.harpocrate.jnisecret"
        minSdkVersion 26
        targetSdkVersion 31
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

    }

    buildTypes {

        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    flavorDimensions "ENV"
    productFlavors {
        dev {
            dimension "ENV"
        }
        preprod {
            dimension "ENV"
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = '1.8'
    }

    externalNativeBuild {
        cmake {
            path = file("CMakeLists.txt")
        }
    }
}

jniSecret {
    packageName "com.harpocrate.sample"
    className "JniSecret"
    generateCMake true

    defaultConfig {
        secret("shared", readProperty("SECRET_SHARED"))
        secret("modifyByDev", readProperty("SECRET_SHARED"))
    }

    productFlavors {
        dev {
            secret("key", readProperty("SECRET_DEV"))
            secret("modifyByDev", readProperty("SECRET_MODIFIED"))
        }
        preprod {
            secret("key1", readProperty("SECRET_PREPROD"))
        }
    }
}

dependencies {

    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.4.31"
    implementation 'androidx.core:core-ktx:1.7.0'
    implementation 'androidx.appcompat:appcompat:1.4.0'
    implementation 'com.google.android.material:material:1.4.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.2'
    testImplementation 'junit:junit:4.+'
    androidTestImplementation 'androidx.test.ext:junit:1.1.3'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'

}
